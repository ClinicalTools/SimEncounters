using UnityEngine;

namespace ClinicalTools.SimEncounters
{
    // Using a class like this is usually not ideal, but this helps with selecting classes in the editor
    public abstract class ReaderBehaviour : MonoBehaviour { }
}
